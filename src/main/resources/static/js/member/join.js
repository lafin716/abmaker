/**
 *  로그인, 회원가입 스크립트
 */
 var joinFrmObj = document.getElementById('joinFrm'); 
 
 
 // 초기화 선언
 document.addEventListener("DOMContentLoaded", init);
 function init(){
 	
 }
 
 // 각 input 마다 메세지 박스 추가해줌
 function generateMsgbox(){
 	let inputList = joinFrmObj.querySelector('input');
 	
 	for(inputObj in inputList){
 		
 	} 
 }  
 
 // 회원가입 시 form 객체 확인 스크립트
 
 